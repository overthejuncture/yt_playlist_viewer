Need to scroll down on "watch later" page
and copy whole html from devtools. If 
you scroll too fast - some images will be skipped, so
you need to scroll through the whole page multiple times
to be sure that all the images are visible on the page.

API is statefull.
